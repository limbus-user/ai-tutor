# AI Tutor 프로젝트 현재 상태 요약

## 프로젝트 목적

이 프로젝트의 목표는 단순 채팅 앱이 아니라, 다음 흐름을 가진 `RAG 기반 AI 튜터`를 만드는 것입니다.

- 업로드한 학습 자료를 근거로 답변하기
- 사용자의 학습 흐름과 대화를 기억하기
- 틀린 문제를 오답노트와 복습으로 연결하기

## 현재 구현된 주요 기능

### 인증 / 사용자

- 회원가입
- 로그인
- JWT 발급
- 사용자 생성 / 조회

### 학습 콘텐츠

- 개념 생성 / 조회
- 문제 생성 / 조회
- 문제 제출
- 정답 / 오답 판정

### 오답 복습 흐름

- 틀린 문제 제출 시 `WrongAnswerNote` 생성
- 복습용 `ReviewQueue` 생성
- 복습 대기 상태 조회 가능

### 채팅 / 학습 메모

- `ChatSession` 생성 / 조회 / 종료
- `ChatMessage` 저장 / 조회
- `LearningMemory` 조회 / 수정

현재 학습 메모에는 다음이 저장됩니다.

- 취약 개념 요약
- 학습 이력 요약
- 설명 선호 방식

### RAG 문서 처리

- PDF 업로드
- 텍스트 추출
- chunk 분리
- 문서 기반 질의응답
- 문서 기반 문제 생성

### 프론트엔드 데모

- `/index.html` 정적 프론트엔드 제공
- 브라우저에서 사용자 생성, 메모 저장, PDF 업로드, 문제 생성, 세션 생성, 튜터 질문, 메시지 조회 가능

### 튜터 응답

- 채팅 세션 안에서 질문 가능
- 문서 근거 기반 답변
- 직전 질문을 일부 반영한 후속 질문 보정
- 답변과 source 저장

## 최근 작업 반영 사항

이번 정리 기준으로 반영된 주요 변경은 아래와 같습니다.

### 1. 문제 조회 / 제출 흐름 개선

- 문제 조회 시 정답과 해설이 바로 보이지 않도록 분리
- 제출 후에만 정답 / 해설 / 다음 액션을 확인하도록 변경

### 2. 문서 기반 질문응답 개선

- `documentId` 기준으로 특정 문서 안에서만 검색 가능
- 개념명 / 설명 문장 중심으로 fallback 검색 품질 개선
- 후속 질문에서 직전 사용자 질문을 일부 참고하도록 보정

### 3. 튜터 응답 포맷 정리

- LLM 답변 안의 출처 표기와 서버 후처리 출처가 중복되던 문제 수정
- 최종 응답은 서버가 정리한 `출처` 형식으로 한 번만 붙도록 개선

### 4. 로컬 LLM 연동

- `Ollama` 기반 로컬 모델 사용 경로 추가
- 현재 데모 권장 모델은 `qwen2.5:3b`
- 16GB RAM 환경에서 `qwen2.5:7b` 보다 안정적으로 동작 가능

### 5. 문서 기반 문제 생성 개선

기존:

- 설명형 문제 몇 개 생성

현재:

- `mixed`
- `multiple_choice`
- `ox`
- `short_answer`

네 가지 생성 모드 지원

응답 구조도 다음 필드까지 포함하도록 확장됨:

- `type`
- `choices`
- `correctAnswer`
- `modelAnswer`
- `explanation`
- `sourceEvidence`
- `difficulty`

### 6. RAG 검색 / 문제 품질 개선

- 임베딩 결과만 쓰지 않고 키워드/개념/문장 점수를 함께 쓰는 하이브리드 검색 적용
- 비슷한 청크 중복 선택을 줄이는 대표 청크 선별 적용
- 문제 생성 시 문서 전체 대신 대표 청크를 먼저 뽑아 근거 품질을 개선
- OX 문항은 항상 참만 내지 않고 일부는 거짓 진술도 생성되도록 보정

## 현재 테스트 상태

자동 테스트 기준:

```powershell
.\gradlew.bat test
```

현재 결과:

- 테스트 통과

수동 테스트 기준으로도 아래 항목은 확인했습니다.

- PDF 업로드 성공
- 문서 기반 문제 생성 성공
- 문서 기반 튜터 질문 성공
- 채팅 세션 저장 / 조회 성공
- 로컬 LLM 응답 생성 성공
- 정적 프론트엔드 페이지 서빙 성공

## 현재 한계

### 1. 문서 질문응답 품질은 아직 고도화 전

- 어떤 PDF는 잘 되고, 어떤 PDF는 구조가 나쁘면 답변 품질이 떨어질 수 있음
- 핵심 원인은 LLM 자체보다 앞단의 RAG 검색 품질

### 2. 벡터 저장은 아직 영속 DB가 아님

- 현재는 메모리 기반 임베딩 저장 구조
- `pgvector` 같은 영속 벡터 저장은 아직 미적용

### 3. 채점 로직은 아직 단순

- 완전 동일 문자열 비교에 가까운 부분이 남아 있음
- 부분 정답 / 유사 답안 / 서술형 평가 고도화 필요

### 4. 후속 질문 문맥 이해는 제한적

- 간단한 `그럼`, `차이점`, `비교` 수준은 일부 보정
- 더 자연스러운 멀티턴 이해는 추가 개선 필요

### 5. 문제 생성 품질은 아직 안정화 중

- 객관식 / OX / 주관식 생성은 가능
- 보기 품질, 난이도 조절, 오답 보기 정교화는 더 다듬어야 함

## 지금 압축 전에 봐야 할 핵심 포인트

현재 프로젝트는 `문서 업로드 -> 문서 기반 질문응답 -> 문제 생성 -> 채팅 세션 기록 -> 오답 복습 흐름` 까지 연결된 `AI 튜터 MVP 백엔드` 상태입니다.

그리고 현재는 `/index.html` 데모 프론트엔드까지 연결되어 있어서, 브라우저에서 핵심 흐름을 한 번에 시연할 수 있습니다.

즉, 시연 가능한 수준의 핵심 흐름은 갖췄고, 앞으로의 핵심 개선 포인트는 다음입니다.

1. RAG 검색 정확도 향상
2. 문제 생성 품질 향상
3. 서술형 채점 고도화
4. 영속 벡터 저장 도입
5. 사용자용 UI 연결
