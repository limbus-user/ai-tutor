# AI Tutor 프로젝트 현재 상태 요약

## 프로젝트 목적

이 프로젝트의 목표는 단순한 채팅 서비스가 아니라, `문서 기반 근거 답변`, `학습 기억`, `오답 복습`을 연결한 `RAG 기반 AI 튜터`를 만드는 것이다.

핵심 목표는 다음 3가지다.

- 업로드한 학습 자료를 근거로 답변하기
- 사용자 학습 상태와 대화 흐름을 기억하기
- 틀린 문제를 기록하고 복습 큐로 연결하기

## 현재 구현된 주요 기능

### 1. 사용자/인증

- 회원가입
- 로그인
- 기본 사용자 생성/조회
- JWT 발급

### 2. 학습 도메인

- `Concept` 생성/조회
- `Problem` 생성/조회
- `UserProblemAttempt` 저장
- 문제 정답/오답 판정

현재 채점 방식은 `문자열 정규화 후 동일성 비교` 기반이다.

### 3. 오답 복습 파이프라인

- 오답 제출 시 `WrongAnswerNote` 생성
- 복습용 `ReviewQueue` 생성
- 복습 완료 처리 API 존재

즉, `문제 제출 -> 오답 기록 -> 복습 큐 생성` 흐름이 연결되어 있다.

### 4. 채팅 및 학습 기억

- `ChatSession` 생성/조회/종료
- `ChatMessage` 저장/조회
- `LearningMemory` 조회/수정

학습 메모에는 다음 정보가 저장된다.

- 취약 개념 요약
- 학습 이력 요약
- 선호 설명 방식

### 5. RAG 문서 처리

- PDF 업로드
- 텍스트 추출
- chunk 분할 저장
- `rag/query` 질의
- `rag/generate-questions` 질문 생성

현재는 `OPENAI_API_KEY`가 있으면 임베딩 검색을 시도하고, 없으면 키워드 기반 검색으로 fallback 한다.

### 6. 튜터 응답 흐름

- 채팅 세션에서 질문 저장
- RAG 검색 실행
- 학습 메모/최근 대화 반영
- 근거 기반 답변 생성
- 답변을 채팅 메시지로 저장

즉, `질문 -> 검색 -> 답변 -> 저장` 흐름이 동작한다.

## 현재까지 확인한 테스트 결과

수동 API 테스트로 아래 항목을 확인했다.

- 회원가입 성공
- 개념 생성 성공
- 문제 생성 성공
- 정답 제출 성공
- 오답 제출 성공
- 오답노트 생성 성공
- 복습 큐 생성 성공
- 채팅 세션 생성 성공
- 채팅 메시지 저장/조회 성공
- 학습 메모 저장 성공
- PDF 업로드 성공
- `rag/query` 응답 성공
- `rag/generate-questions` 응답 성공
- `tutor/ask` 응답 및 채팅 저장 성공

## 자동 테스트 추가 상태

자동 테스트가 없던 상태에서, 현재는 핵심 흐름을 검증하는 통합 테스트가 추가되었다.

추가된 테스트 범위:

- 회원가입/로그인 JWT 발급
- 오답 제출 시 `WrongAnswerNote`, `ReviewQueue` 생성
- PDF 업로드 -> `rag/query` -> `tutor/ask` -> 채팅 저장

실행 기준:

```powershell
.\gradlew.bat test
```

현재 결과:

- 통합 테스트 3건 통과
- 실패 0건

## 확인된 이슈와 한계

### 1. 검색 범위 필터가 없음

현재 RAG 검색은 `업로드된 전체 문서 풀`에서 수행된다.

즉, 방금 업로드한 PDF만 대상으로 제한하지 않기 때문에, 이전에 업로드한 다른 PDF source가 같이 응답에 들어갈 수 있다.

이 부분은 다음 단계에서 `문서별`, `과목별`, `단원별` 필터가 필요하다.

### 2. 벡터 저장이 영속적이지 않음

현재 임베딩은 `InMemoryEmbeddingStore` 기반이다.

- 벡터 DB 미사용
- `pgvector` 미적용
- 재기동 시 영속 벡터 검색 구조가 아님

### 3. 문제 채점이 단순함

현재는 단답 문자열 동일성 비교만 지원한다.

향후 필요:

- 객관식
- 부분 정답
- 서술형 평가

### 4. 튜터 응답 포맷이 단순함

현재 답변은 근거 기반 fallback 구조는 갖췄지만, 제품 수준으로 다듬어진 응답 계약은 아직 아니다.

향후 필요:

- 짧은 답
- 근거 요약
- 자세한 설명
- 후속 질문
- source 구조화

### 5. 일부 레거시 모듈 존재

기존 `LearningSessionController`, `LearningSessionService`, `WrongAnswerService`, `WeaknessAnalysisService` 흐름은 여전히 남아 있다.

이 흐름은 현재 주 아키텍처가 아니라 `레거시/스캐폴딩`에 가깝다.

## 실행 및 검증 상태

확인 완료:

```powershell
.\gradlew.bat compileJava
.\gradlew.bat build
.\gradlew.bat test
```

결과:

- compile 통과
- build 통과
- test 통과

## 다음 우선 작업

우선순위는 아래 순서가 적절하다.

1. `pgvector` 기반 영속 벡터 저장 도입
2. RAG 검색 정확도 개선
3. 문서별/과목별/단원별 검색 필터 추가
4. 튜터 응답 포맷 개선
5. 문제 채점 방식 고도화
6. 자동 테스트 범위 확대

## 현재 상태 한 줄 요약

현재 프로젝트는 `문서 업로드 + 문서 기반 검색 + 질문 생성 + 튜터 응답 + 채팅/학습 기억 + 오답 복습`까지 연결된 초기 AI 튜터 백엔드 단계이며, 핵심 기능은 수동 테스트와 자동 테스트 기준으로 동작 확인이 끝난 상태다.
