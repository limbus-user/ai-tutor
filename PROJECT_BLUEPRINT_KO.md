# AI 튜터 프로젝트 설계도

## 1. 프로젝트 목적

이 프로젝트는 단순한 일반형 챗봇을 만드는 것이 아닙니다.
목표는 `RAG 기반 AI 튜터`를 만드는 것입니다.

이 프로젝트는 신뢰할 수 있는 학습 자료를 근거로 답변하여 환각 현상을 줄이고, 사용자의 학습 맥락을 세션이 바뀌어도 이어서 기억하며, 틀린 문제를 다시 복습하게 만드는 개인화 학습 시스템을 지향합니다.

핵심 목표는 3가지입니다.

1. `환각 최소화`
   - AI가 막연한 일반 지식에 의존하지 않도록 합니다.
   - 검증된 교재, 개념 자료, 문제, 해설 데이터를 우선 근거로 사용합니다.

2. `대화 연속성 유지`
   - 일반 챗봇처럼 세션이 끊길 때 맥락이 사라지는 것이 아니라,
     학습자의 최근 대화, 학습 진도, 취약 개념, 복습 이력을 이어서 기억해야 합니다.

3. `오답 복습 지원`
   - 학생이 문제를 틀렸을 때 단순히 정답만 보여주고 끝나는 것이 아니라,
     왜 틀렸는지 설명하고, 관련 개념을 연결하고, 이후 복습까지 이어지게 해야 합니다.

한 줄로 정리하면:

`이 프로젝트는 RAG를 이용해 근거 기반으로 답변하고, 학습자 상태를 기억하며, 오답 복습 루프까지 수행하는 개인화 AI 튜터 시스템이다.`

## 2. 현재 구현 상태

현재 코드베이스는 아직 AI 튜터 전체가 아니라, 인증과 사용자 관리를 위한 백엔드 기초 단계입니다.

현재 구현된 내용:

- Spring Boot 애플리케이션 기본 구성
- PostgreSQL 연동
- JPA 기반 사용자 저장
- JWT 기반 인증
- 회원가입 / 로그인 API
- 인증이 필요한 사용자 API

아직 구현되지 않은 내용:

- RAG 문서 수집 및 적재
- 벡터 검색
- LLM 연동
- 채팅 세션 저장
- 장기 학습 메모리
- 문제 풀이 흐름
- 오답 파이프라인
- 복습 스케줄링
- 학습 분석 기능
- 프론트엔드
- 자동 테스트

즉, 현재 프로젝트 상태는 `AI 튜터 완성본`이 아니라 `인증이 포함된 백엔드 골격` 단계입니다.

## 3. 최종 제품 정의

최종적으로 이 프로젝트는 아래와 같이 동작해야 합니다.

1. 학생이 질문을 하거나 문제를 풉니다.
2. 시스템은 신뢰 가능한 학습 자료와 문제 해설에서 관련 정보를 RAG로 검색합니다.
3. AI는 검색된 근거를 바탕으로만 최대한 답변합니다.
4. 대화 내용은 저장되어 다음 세션에서도 이어집니다.
5. 학생이 문제를 틀리면 오답 기록이 남고, 취약 개념이 연결되며, 복습이 예약됩니다.

이 프로젝트는 아래 3개 레이어가 함께 동작해야 의미가 있습니다.

- `근거 기반 답변`
- `지속되는 학습 맥락`
- `오답 중심 복습 튜터링`

## 4. 전체 아키텍처

권장 전체 흐름:

`클라이언트 -> Spring Boot API -> 도메인 서비스 -> RAG 검색 -> LLM -> DB 저장 -> 복습 파이프라인`

주요 모듈은 다음과 같이 나눕니다.

- `Auth/User`
  - 회원가입, 로그인, JWT, 사용자 식별

- `Content`
  - 개념, 문서, 문제, 해설, 메타데이터 관리

- `RAG`
  - chunk 분리, 임베딩, 벡터 검색, 근거 추출

- `Chat`
  - 채팅 세션, 메시지 저장, 최근 대화 문맥

- `Learning Memory`
  - 장기 학습 기억, 취약 개념, 설명 선호도

- `Tutoring`
  - 질문 응답, 해설, 힌트, 단계별 풀이 지원

- `Review Pipeline`
  - 오답 기록, 개념 진단, 복습 큐 관리

- `Analytics`
  - 정답률, 취약 영역, 복습 완료율, 학습 진도

## 5. 왜 RAG가 반드시 필요한가

이 프로젝트의 핵심 차별점은 일반 생성형 AI보다 환각을 줄이는 것입니다.
따라서 모델 자체를 진실의 원천처럼 쓰면 안 됩니다.

정답 흐름은 반드시 이렇게 가야 합니다.

1. 사용자 질문을 받는다.
2. 신뢰 가능한 학습 자료에서 관련 chunk를 검색한다.
3. 검색된 근거와 사용자 문맥만 모델에 전달한다.
4. 모델은 그 근거 안에서 답변한다.
5. 답변과 함께 출처 또는 근거 식별자를 반환한다.

핵심 원칙:

`모델의 사전지식보다 검색된 근거를 우선한다.`

검색 신뢰도가 낮을 경우에는 아래 방식이 더 바람직합니다.

- 추가 질문하기
- 불확실하다고 명시하기
- 근거 부족 시 답변을 제한하기

근거 없는 내용을 그럴듯하게 만들어내면 안 됩니다.

## 6. 대화 연속성 설계

대화 맥락이 사라지지 않게 하려면 메모리를 2단계로 나눠야 합니다.

### 단기 메모리

자연스러운 현재 대화를 위한 메모리입니다.

- 현재 세션의 최근 메시지
- 지금 풀고 있는 문제
- 직전 힌트와 응답

### 장기 학습 메모리

세션이 바뀌어도 유지되는 개인화용 기억입니다.

- 취약 개념
- 최근 틀린 문제
- 선호하는 설명 방식
- 현재 학습 중인 단원
- 쌓여 있는 복습 목록

매 요청 시 프롬프트에는 아래만 조합하는 것이 좋습니다.

- 최근 대화 문맥
- 장기 학습 메모리 요약
- RAG 검색 근거

전체 대화 이력을 매번 다 넣지 말고,
요약과 제한된 컨텍스트 윈도우를 사용해야 합니다.

## 7. 오답 복습 파이프라인

이 부분이 프로젝트의 핵심 차별 포인트입니다.

권장 흐름:

1. 학생이 문제 답안을 제출한다.
2. 시스템이 정답/오답을 판정한다.
3. 오답이면 시도 기록을 저장한다.
4. 어떤 개념 때문에 틀렸는지 연결한다.
5. 신뢰 가능한 자료를 기반으로 해설을 생성한다.
6. 후속 복습 항목을 생성한다.
7. 나중에 유사 문제 또는 선수 개념 문제를 다시 제시한다.
8. 개념이 안정될 때까지 반복한다.

이 파이프라인은 다음을 지원해야 합니다.

- 즉시 피드백
- 개념 기반 보완
- 반복 연습
- 간격 반복 복습

오답은 단발성 결과가 아니라 `학습 신호`로 다뤄야 합니다.

## 8. 권장 도메인 모델

실제 제품을 위해 최소한 필요한 엔티티는 다음과 같습니다.

### 이미 존재함

- `User`

### 다음에 필요한 것

- `Concept`
  - 과목, 단원, 개념명, 설명

- `Document`
  - 문서 제목, 문서 종류, 신뢰도, 과목, 단원

- `DocumentChunk`
  - 문서 참조, chunk 본문, 임베딩 참조, 메타데이터

- `Problem`
  - 문제 본문, 정답, 해설, 난이도, 유형, 관련 개념

- `ChatSession`
  - 사용자, 제목, 상태, 생성 시각, 수정 시각

- `ChatMessage`
  - 세션, 역할, 내용, 출처 참조, 생성 시각

- `UserProblemAttempt`
  - 사용자, 문제, 제출 답안, 정오답 여부, 피드백, 시각

- `WrongAnswerNote`
  - 사용자, 시도 기록, 연결 개념, 해설, 복습 상태

- `ReviewQueue`
  - 사용자, 개념 또는 문제 참조, 다음 복습 시각, 우선순위, 상태

- `LearningMemory`
  - 사용자, 취약 개념 요약, 학습 이력 요약, 선호도

## 9. API 방향

MVP 기준 추천 API:

### 인증

- `POST /api/auth/signup`
- `POST /api/auth/login`

### 채팅

- `POST /api/chat/sessions`
- `GET /api/chat/sessions/{sessionId}`
- `GET /api/chat/sessions/{sessionId}/messages`
- `POST /api/chat/sessions/{sessionId}/messages`

### 문제

- `POST /api/problems`
- `GET /api/problems/{problemId}`
- `POST /api/problems/{problemId}/submit`

### 복습

- `GET /api/reviews`
- `POST /api/reviews/{reviewId}/solve`

### RAG

- `POST /api/rag/documents`
- `POST /api/rag/query`

### 분석

- `GET /api/analytics/weaknesses`

## 10. 권장 기술 방향

현재 백엔드 기술 스택은 이미 아래 방향과 잘 맞습니다.

- Java 17
- Spring Boot
- Spring Security
- Spring Data JPA
- PostgreSQL
- JWT

추가 권장 기술:

- PostgreSQL 기반 벡터 저장을 위한 `pgvector`
- 근거 기반 응답 생성을 위한 LLM API 연동
- 문서 파싱 및 임베딩 적재 파이프라인

현실적인 1차 버전 권장 조합:

`Spring Boot + PostgreSQL + pgvector + 외부 LLM API`

이 조합은 현재 구조를 크게 흔들지 않으면서도 RAG를 붙이기 좋습니다.

## 11. 구현 우선순위

처음부터 모든 것을 한 번에 만들지 말고 다음 순서로 진행합니다.

### 1단계: 백엔드 기초 안정화

- 현재 인증/사용자 흐름 유지
- 입력 검증과 예외 처리 추가
- 회원가입/로그인 테스트 작성

### 2단계: 학습 도메인 추가

- `Concept` 추가
- `Problem` 추가
- `UserProblemAttempt` 추가
- 문제 제출 및 채점 API 구현

### 3단계: 오답 파이프라인 추가

- `WrongAnswerNote` 추가
- `ReviewQueue` 추가
- 오답 시 복습 큐 연결

### 4단계: 대화 연속성 추가

- `ChatSession` 추가
- `ChatMessage` 추가
- 학습 메모리 요약 모델 추가

### 5단계: RAG 추가

- 문서 적재
- chunk 분리
- 임베딩 생성
- 벡터 검색
- 근거 기반 프롬프트 조합

### 6단계: 튜터링 지능 추가

- 근거 기반 답변 생성
- 힌트 생성
- 개념 기반 보충 설명
- 답변 출처 표시

### 7단계: 분석 및 고도화

- 취약 개념 대시보드
- 복습 완료율 추적
- 프롬프트 품질 개선
- 환각 감소 정책 고도화

## 12. 반드시 지켜야 할 설계 원칙

이 프로젝트를 이어받는 어떤 에이전트라도 아래 원칙은 지켜야 합니다.

1. 이 프로젝트를 일반형 잡담 챗봇으로 바꾸지 말 것
2. RAG는 반드시 신뢰 가능한 학습 자료에 기반할 것
3. 대화 연속성을 세션 간에도 유지할 것
4. 오답은 단순 채점 결과가 아니라 복습 신호로 처리할 것
5. 근거 없는 유창한 답변보다 출처가 있는 답변을 우선할 것
6. 인증, 튜터링, RAG, 복습 기능은 모듈적으로 분리할 것

## 13. 다음 에이전트가 바로 해야 할 일

이 파일을 읽고 이어서 개발하는 에이전트는 다음 순서로 작업하는 것이 좋습니다.

1. 현재 인증 중심 백엔드 구조를 다시 확인한다.
2. 첫 번째 핵심 학습 엔티티를 추가한다.
   - `Concept`
   - `Problem`
   - `UserProblemAttempt`
3. 문제 제출과 정오답 판정 기능을 구현한다.
4. 오답 저장과 간단한 복습 큐를 구현한다.
5. 그 다음 첫 번째 RAG 적재/검색 모듈을 시작한다.

## 14. 최종 요약

이 저장소는 최종적으로 아래와 같은 시스템으로 발전해야 합니다.

`환각을 줄이기 위해 RAG를 사용하고, 학습자의 맥락을 세션 간에도 기억하며, 틀린 문제를 자동으로 복습하게 만드는 개인화 AI 튜터 시스템`

현재 구현은 인증과 사용자 관리 기초 레이어에 해당합니다.
이후에는 그 위에 튜터링, 메모리, RAG, 오답 복습 레이어를 순차적으로 쌓아야 합니다.
